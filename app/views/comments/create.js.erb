$('#comments').prepend("<%= j render 'shared/comment', comment: @comment, hide_counter: true %>");
$('#comment_description').val('');

document.querySelector("#last-comment-wrapper-<%= @comment.content.id %>").innerHTML = "<%= j render 'shared/comment', comment: @comment, hide_counter: false %>";

swipePost();
modal();
