<div class="modal">
  <div class="modal__header">
    <div class="modal__title">
      <%= content.description %>
    </div>
    <button data-close-modal="#closeModal"  class="modal__close">&times;</button>
  </div>
  <div class="modal__form">
    <%= render 'shared/commentForm', content: content, comment: @comment %>
  </div>
  <div class="modal__body" id="comments">
    <% content.comments.order(created_at: :desc).each do |comment|%>
      <% if comment %>
        <%= render "shared/comment", comment: comment, hide_counter: true %>
      <% end%>
    <% end%>
  </div>
</div>
