<% if !request.params[:controller].include?("devise") %>
  <div class="navbar">
    <div>
      <%= link_to root_path, class: "navbar__logo" do %>
          ogram
      <% end %>
    </div>

    <div>
      <% if params[:controller] == "contents" %>
      <%= render 'shared/searchBar', path: contents_path %>
      <% else %>
      <%= render 'shared/searchBar', path: root_path %>
      <% end %>
    </div>
  </div>
<% end %>
